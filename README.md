# URL Shortner in Go

Build and install.

```shell
$ go install
```

Launch app.

```shell
$ $GOPATH/bin/go-url-shortner
```

Create a new short URL.

<http://localhost:8080/create>

List short URLs.

<http://localhost:8080/list>
